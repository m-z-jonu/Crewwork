# Fix Jitsi Lobby Bypass — Keep Cloud Jitsi

> **Problem:** Jitsi's free cloud shows "Log in to become a moderator" lobby. The `startWithModerator` config doesn't work because the lobby is enforced server-side.

> **Solution:** Disable the lobby entirely with `enableLobby: false` in the Jitsi config overrides. This makes the free cloud work without any login.

---

## Implementation

### Task 1: Update Jitsi config overrides

In `src/components/calls/call-panel.tsx`, update the `configOverwrite` object to include:

```typescript
configOverwrite: {
  startWithAudioMuted: false,
  startWithVideoMuted: false,
  prejoinPageEnabled: false,
  enableLobby: false,                    // DISABLE LOBBY
  startWithModertor: true,               // Make first user moderator
  enableInsecureRoomNameWarning: false,   // Suppress warnings
  disableDeepLinking: true,
  defaultLanguage: 'en',
  toolbarButtons: [...],
},
```

Also update `interfaceConfigOverwrite`:
```typescript
interfaceConfigOverwrite: {
  SHOW_JITSI_WATERMARK: false,
  SHOW_BRAND_WATERMARK: false,
  SHOW_WATERMARK_FOR_GUESTS: false,
  SHOW_POWERED_BY: false,
  TOOLBAR_ALWAYS_VISIBLE: true,
  DEFAULT_BACKGROUND: '#ffffff',
  TOOLBAR_COLOR: '#DC2626',
  TOOLBAR_BG_COLOR: '#ffffff',
  TOOLBAR_TEXT_COLOR: '#1C1917',
  DISABLE_JOIN_LEAVE_NOTIFICATIONS: true,
  SHOW_CHROME_EXTENSION_BANNER: false,    // Suppress Chrome extension banner
},
```

### Task 2: Verify and test

1. Start a call → should connect directly without lobby
2. Test with two users in the same room
3. Verify video/audio works

---

## Files to Modify

| File | Change |
|------|--------|
| `src/components/calls/call-panel.tsx` | Add `enableLobby: false` + `enableInsecureRoomNameWarning: false` to config |

## Verification

1. Click phone icon → lobby screen appears (our custom lobby)
2. Click "Join Call" → Jitsi connects directly (no Jitsi lobby)
3. No "Log in" button visible
4. Video/audio controls work
