'use client'

// Jitsi Meet is always available — no setup required
export function CallSetupDialog({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void }) {
  return null
}
